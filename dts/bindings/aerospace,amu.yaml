description: AMU device driver

compatible: "aerospace,amu"

# FILL IN: include bus binding if the device sits on a bus, e.g.:
# include: i2c-device.yaml

properties:
  # FILL IN: devicetree properties (mirror struct amu_config fields)
